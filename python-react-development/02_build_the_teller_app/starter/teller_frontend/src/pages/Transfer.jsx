import React from 'react';

// TODO (SPEC.md — Transfer): a form to transfer funds between two IBANs with an
// amount, a confirmation modal for this critical action, and clear success/error
// notifications. Call transferByIban(sourceIban, destinationIban, amount). The
// backend rejects over-balance transfers with HTTP 403 + a detail message — show it.
const Transfer = () => <h2>Transfer — TODO: build per SPEC.md</h2>;

export default Transfer;
