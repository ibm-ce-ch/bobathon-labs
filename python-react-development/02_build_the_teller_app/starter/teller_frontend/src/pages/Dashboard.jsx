import React from 'react';

// TODO (SPEC.md — Dashboard): list the teller's accounts in a Carbon DataTable
// (searchable/filterable by IBAN, paginated — there are ~1000), with a "View
// Details" action per row that routes to /accounts/:accountId.
// Data: getAccounts() from ../services/api.
const Dashboard = () => <h2>Dashboard — TODO: build per SPEC.md</h2>;

export default Dashboard;
