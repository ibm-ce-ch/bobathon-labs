import React from 'react';

// TODO (SPEC.md — Overdraft Request): a form (IBAN + amount 0–10,000 EUR) that
// produces a formatted back-office request message. This is client-side only — a
// teller cannot grant overdrafts. Use buildOverdraftRequest(iban, amount) from
// ../services/api and show the result (e.g. in a modal / read-only text area).
const OverdraftRequest = () => <h2>Overdraft Request — TODO: build per SPEC.md</h2>;

export default OverdraftRequest;
